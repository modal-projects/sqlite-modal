[project]
name = "distributed-sqlite"
version = "0.1.0"
description = "Multi-tenant SQLite on Modal — exclusive writer per named DB"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.5",
    "fastapi[standard]>=0.139.0",
    "matplotlib>=3.11.0",
    "modal>=1.5.2",
    "sqlalchemy>=2.0.51",
]

[tool.uv]
package = false
